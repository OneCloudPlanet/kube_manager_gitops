apiVersion: v1
kind: ConfigMap
metadata:
  name: environment-configs
  namespace: flux-system
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: merge
data:
  DOMAIN: "ocplanet.cloud"
  PROJECT: "kube-manager"
  ENVIRONMENT: "kube-dev"
  HOST: "k8s-dev.ocplanet.cloud"
  EMAIL_DOMAIN: "info@onecloudplanet.com"
  CLOUDFLARE_EMAIL: "info@onecloudplanet.com"
  ISTIO_VERSION: "1.24.2"
  # ── DB backups
  CNPG_BUCKET: "k8s-dev-backup"
  CNPG_ENDPOINTURL: "https://k8s-dev-backup.s3.ua-central-1.ocplanet.cloud"
  # ── Loki
  LOKI_BUCKET: "k8s-dev-metrics"
  LOKI_ENDPOINT: "https://k8s-dev-metrics.s3.ua-central-1.ocplanet.cloud"
  # ── Tempo
  TEMPO_S3_BUCKET: "k8s-dev-metrics"
  TEMPO_S3_ENDPOINT: "https://k8s-dev-metrics.s3.ua-central-1.ocplanet.cloud"
  # ── Thanos
  THANOS_BUCKET: "k8s-dev-metrics"
  THANOS_ENDPOINT: "https://k8s-dev-metrics.s3.ua-central-1.ocplanet.cloud"
